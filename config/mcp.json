{
  "_comment": "Sanitized inventory of locally-configured MCP servers, written by /harness:update-project. Secrets (tokens, API keys, passwords) are redacted to ${PLACEHOLDER} — restore is manual (`claude mcp add ...` or merge into ~/.claude.json) and you must supply the real values.",
  "mcpServers": {
    "brightdata": {
      "type": "sse",
      "url": "https://mcp.brightdata.com/sse?token=${BRIGHTDATA_TOKEN}"
    },
    "playwright": {
      "type": "local",
      "command": ["npx", "-y", "@playwright/mcp"],
      "enabled": true
    }
  }
}
