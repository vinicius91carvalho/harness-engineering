{
  "_comment": "Sanitized inventory of locally-configured MCP servers, written by /harness:update-project. Secrets (tokens, API keys, passwords) are redacted to ${PLACEHOLDER}; the installer prompts for them before configuring each selected host.",
  "mcpServers": {
    "brightdata": {
      "type": "http",
      "url": "https://mcp.brightdata.com/mcp?token=${BRIGHTDATA_TOKEN}"
    },
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@playwright/mcp"]
    }
  }
}
