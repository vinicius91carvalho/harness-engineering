---
description: Portable harness coding-agent role using the configured OpenCode model
mode: subagent
---

Follow the repository definition in `agents/coding-agent.md`. Work only in the supplied
worktree and report observable completion through `feature_list.json`.
