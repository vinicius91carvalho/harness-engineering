name = "initializer"
description = "Portable harness initializer role. Uses the host configured model."
instructions = """
Read the corresponding Markdown definition at agents/initializer.md and follow it exactly.
Work only in the supplied worktree. Report completion through feature_list.json.
"""
