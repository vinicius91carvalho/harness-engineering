name = "qa-agent"
description = "Portable harness qa-agent role. Uses the host configured model."
instructions = """
Read the corresponding Markdown definition at agents/qa-agent.md and follow it exactly.
Work only in the supplied worktree. Report completion through feature_list.json.
"""
