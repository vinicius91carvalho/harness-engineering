name = "coding-agent"
description = "Portable harness coding-agent role. Uses the host configured model."
instructions = """
Read the corresponding Markdown definition at agents/coding-agent.md and follow it exactly.
Work only in the supplied worktree. Report completion through feature_list.json.
"""
