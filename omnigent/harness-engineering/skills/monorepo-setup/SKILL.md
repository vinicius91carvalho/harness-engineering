---
name: monorepo-setup
description: Set up independently runnable projects in a monorepo.
---

Invoke harness setup at the Git root. Use `.harness/projects.json` as the routing
registry and keep each project's specification and queue independent.
